const translations = {
  en: {
    navFeatures: "Features",
    navComparison: "Comparison",
    navShowcase: "Showcase",
    navVideos: "Videos",
    navDemo: "Request Demo",
    eyebrow: "One system. One journey. Endless possibilities.",
    heroTitle: "Run finance, operations, and growth from one premium ERP experience.",
    heroText: "Odyssey ERP replaces scattered spreadsheets, slow approvals, and disconnected teams with a fast, elegant platform built for modern businesses.",
    heroPrimary: "Request a Demo",
    heroSecondary: "Explore Product Screens",
    metricOne: "Unified business visibility",
    metricTwo: "Platform for finance and operations",
    metricThree: "Decision-ready insights",
    trustText: "Designed for organizations that want enterprise-grade control with a clean, modern user experience.",
    featuresEyebrow: "Why Odyssey ERP",
    featuresTitle: "Everything leadership teams need to operate with clarity.",
    feature1Title: "Executive dashboards",
    feature1Text: "Track cash position, receivables, payables, approvals, and top customers in one live command center.",
    feature2Title: "Financial reporting",
    feature2Text: "Move from manual reconciliation to instant reports, structured statements, and clear audit visibility.",
    feature3Title: "Expense intelligence",
    feature3Text: "Spot trends by category, branch, and budget variance before costs become problems.",
    feature4Title: "Faster approvals",
    feature4Text: "Reduce delays across purchasing, invoicing, payments, journal entries, and employee workflows.",
    comparisonEyebrow: "Traditional vs. Odyssey",
    comparisonTitle: "Replace fragmented management with one connected operating system.",
    oldWayTitle: "Traditional business management",
    old1: "Scattered spreadsheets and duplicated data",
    old2: "Delayed approvals and manual follow-up",
    old3: "Limited visibility into performance",
    old4: "Slow month-end reporting cycles",
    old5: "Teams working in disconnected tools",
    newWayTitle: "Odyssey ERP",
    new1: "One source of truth across finance and operations",
    new2: "Structured approvals with real-time status",
    new3: "Instant dashboards for better decisions",
    new4: "Reporting that is fast, accurate, and presentation-ready",
    new5: "A modern experience teams actually enjoy using",
    showcaseEyebrow: "Product Showcase",
    showcaseTitle: "Real Odyssey ERP screens, presented exactly as they are.",
    showcaseText: "We preserved the original product screenshots and placed them in a premium browsing experience.",
    screen1Tag: "Operations overview",
    screen1Title: "Dashboard",
    screen1Text: "Leadership-ready visibility across cash, approvals, customer activity, and cash flow trends.",
    screen2Tag: "Cost control",
    screen2Title: "Expense Analysis",
    screen2Text: "Analyze spending by branch, category, and budget performance with clear visual breakdowns.",
    screen3Tag: "Financial clarity",
    screen3Title: "Financial Reports",
    screen3Text: "Review structured statements and export-ready reports with confidence and speed.",
    videosEyebrow: "Product Videos",
    videosTitle: "See Odyssey ERP in motion.",
    videosText: "Short product clips help prospects understand the experience before they book a walkthrough.",
    video1Tag: "Experience preview",
    video1Title: "Premium product overview",
    video2Tag: "Guided walkthrough",
    video2Title: "Odyssey ERP product demo clip",
    ctaEyebrow: "Book your walkthrough",
    ctaTitle: "See how Odyssey ERP can modernize your business management.",
    ctaText: "Give your team a single platform for finance, control, reporting, and operational momentum.",
    formName: "Full name",
    formCompany: "Company",
    formEmail: "Work email",
    formPhone: "Phone / WhatsApp",
    formNamePlaceholder: "Your name",
    formCompanyPlaceholder: "Company name",
    formEmailPlaceholder: "name@company.com",
    formPhonePlaceholder: "+20 000 000 0000",
    formSubmit: "Request Demo",
    formNote: "We'll use these details to contact you and arrange a tailored demo.",
    formLoading: "Sending your request securely...",
    formSuccess: "Thank you. Your demo request was sent successfully and our team will contact you shortly.",
    formError: "We couldn't send your request right now. Please try again in a moment.",
    formDuplicate: "This request was already sent recently. Please wait a few minutes before sending it again.",
    formInvalidRequired: "Please complete all required fields before submitting.",
    formInvalidEmail: "Please enter a valid work email address.",
    formConfigError: "The demo form is not connected yet. Paste the Google Apps Script Web App URL into lead-form.config.js.",
    footerText: "Odyssey ERP. One system. One journey. Endless possibilities."
  },
  ar: {
    navFeatures: "المزايا",
    navComparison: "المقارنة",
    navShowcase: "الواجهة",
    navVideos: "الفيديوهات",
    navDemo: "اطلب عرضًا",
    eyebrow: "نظام واحد. رحلة واحدة. إمكانيات بلا حدود.",
    heroTitle: "شغّل الإدارة المالية والعمليات والنمو من خلال تجربة ERP متكاملة وفاخرة.",
    heroText: "يستبدل Odyssey ERP الجداول المتفرقة، والموافقات البطيئة، والفرق غير المترابطة بمنصة سريعة وعصرية مصممة للشركات الحديثة.",
    heroPrimary: "اطلب عرضًا توضيحيًا",
    heroSecondary: "استكشف شاشات النظام",
    metricOne: "رؤية موحدة للأعمال",
    metricTwo: "منصة واحدة للمالية والعمليات",
    metricThree: "رؤى جاهزة لاتخاذ القرار",
    trustText: "مصمم للمؤسسات التي تريد تحكمًا بمستوى الشركات الكبرى مع تجربة استخدام حديثة وواضحة.",
    featuresEyebrow: "لماذا Odyssey ERP",
    featuresTitle: "كل ما تحتاجه الإدارة لتشغيل الأعمال بوضوح وثقة.",
    feature1Title: "لوحات تنفيذية",
    feature1Text: "تابع النقدية، الذمم، الموافقات، وأفضل العملاء من خلال مركز قيادة مباشر.",
    feature2Title: "تقارير مالية",
    feature2Text: "انتقل من التسويات اليدوية إلى تقارير فورية وقوائم منظمة ورؤية أوضح للتدقيق.",
    feature3Title: "ذكاء المصروفات",
    feature3Text: "اكتشف الاتجاهات حسب الفئة والفرع والانحراف عن الميزانية قبل أن تتحول التكاليف إلى مشكلة.",
    feature4Title: "موافقات أسرع",
    feature4Text: "قلّل التأخير في المشتريات، والفواتير، والمدفوعات، والقيود اليومية، وطلبات الموظفين.",
    comparisonEyebrow: "التقليدي مقابل Odyssey",
    comparisonTitle: "استبدل الإدارة المجزأة بنظام تشغيل أعمال مترابط في منصة واحدة.",
    oldWayTitle: "الإدارة التقليدية للأعمال",
    old1: "جداول متفرقة وبيانات مكررة",
    old2: "موافقات متأخرة ومتابعات يدوية",
    old3: "رؤية محدودة للأداء",
    old4: "إغلاق مالي وتقارير بطيئة",
    old5: "فرق تعمل في أدوات منفصلة",
    newWayTitle: "Odyssey ERP",
    new1: "مصدر موحد للحقيقة عبر المالية والعمليات",
    new2: "مسارات موافقات منظمة مع حالة فورية",
    new3: "لوحات لحظية لاتخاذ قرارات أفضل",
    new4: "تقارير سريعة ودقيقة وجاهزة للعرض",
    new5: "تجربة حديثة يحب الفريق العمل عليها",
    showcaseEyebrow: "استعراض المنتج",
    showcaseTitle: "شاشات Odyssey ERP الحقيقية كما هي، بدون أي تعديل على التصميم.",
    showcaseText: "حافظنا على لقطات الشاشة الأصلية وقدمناها داخل تجربة عرض راقية.",
    screen1Tag: "نظرة تشغيلية",
    screen1Title: "لوحة التحكم",
    screen1Text: "رؤية مناسبة للإدارة حول النقدية والموافقات ونشاط العملاء واتجاهات التدفقات النقدية.",
    screen2Tag: "التحكم في التكاليف",
    screen2Title: "تحليل المصروفات",
    screen2Text: "حلّل الإنفاق حسب الفرع والفئة وأداء الميزانية من خلال عرض بصري واضح.",
    screen3Tag: "وضوح مالي",
    screen3Title: "التقارير المالية",
    screen3Text: "راجع القوائم المنظمة والتقارير الجاهزة للتصدير بسرعة وثقة.",
    videosEyebrow: "فيديوهات المنتج",
    videosTitle: "شاهد Odyssey ERP أثناء العمل.",
    videosText: "مقاطع قصيرة تساعد العملاء المحتملين على فهم التجربة قبل حجز العرض التوضيحي.",
    video1Tag: "نظرة سريعة",
    video1Title: "استعراض بصري للمنتج",
    video2Tag: "جولة إرشادية",
    video2Title: "مقطع عرض لنظام Odyssey ERP",
    ctaEyebrow: "احجز جولتك",
    ctaTitle: "اكتشف كيف يطوّر Odyssey ERP إدارة أعمالك.",
    ctaText: "امنح فريقك منصة واحدة للمالية، والرقابة، والتقارير، وتسريع العمليات.",
    formName: "الاسم الكامل",
    formCompany: "الشركة",
    formEmail: "البريد الوظيفي",
    formPhone: "الهاتف / واتساب",
    formNamePlaceholder: "اسمك",
    formCompanyPlaceholder: "اسم الشركة",
    formEmailPlaceholder: "name@company.com",
    formPhonePlaceholder: "+20 000 000 0000",
    formSubmit: "اطلب عرضًا",
    formNote: "سنستخدم هذه البيانات للتواصل معك وترتيب عرض توضيحي مناسب.",
    formLoading: "جارٍ إرسال طلبك بأمان...",
    formSuccess: "شكرًا لك. تم إرسال طلب العرض التوضيحي بنجاح وسيتواصل معك فريقنا قريبًا.",
    formError: "تعذر إرسال طلبك الآن. يرجى المحاولة مرة أخرى بعد قليل.",
    formDuplicate: "تم إرسال هذا الطلب مؤخرًا بالفعل. يرجى الانتظار بضع دقائق قبل الإرسال مرة أخرى.",
    formInvalidRequired: "يرجى إكمال جميع الحقول المطلوبة قبل الإرسال.",
    formInvalidEmail: "يرجى إدخال بريد وظيفي صحيح.",
    formConfigError: "نموذج العرض غير متصل بعد. الصق رابط Web App الخاص بـ Google Apps Script داخل lead-form.config.js.",
    footerText: "Odyssey ERP. نظام واحد. رحلة واحدة. إمكانيات بلا حدود."
  }
};

const root = document.documentElement;
const body = document.body;
const langToggle = document.getElementById("langToggle");
const themeToggle = document.getElementById("themeToggle");
const menuToggle = document.getElementById("menuToggle");
const nav = document.querySelector(".nav");
const form = document.getElementById("demoForm");
const formMessage = document.getElementById("formMessage");
const submitButton = document.getElementById("formSubmitButton");
const hiddenFrame = document.getElementById("leadCaptureFrame");
const dynamicShowcaseMount = document.getElementById("dynamicShowcaseMount");
const fields = Array.from(form.querySelectorAll("input:not([type='hidden'])"));

const APP_CONFIG = window.ODYSSEY_DEMO_CONFIG || {};
const APP_SCRIPT_URL = (APP_CONFIG.webAppUrl || "").trim();
const DUPLICATE_WINDOW_MS = 5 * 60 * 1000;
const RESPONSE_TIMEOUT_MS = 25000;
const savedLanguage = localStorage.getItem("odyssey-language") || "en";
const savedTheme = localStorage.getItem("odyssey-theme");
const systemPrefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;

let submitTimeoutId = null;
let pendingSubmissionKey = "";
let isSubmitting = false;

function prettifyFileName(fileName) {
  return String(fileName || "")
    .replace(/\.[^.]+$/, "")
    .replace(/[_-]+/g, " ")
    .replace(/\s+/g, " ")
    .trim();
}

function renderAutoGallery() {
  if (!dynamicShowcaseMount) return;

  const items = Array.isArray(window.ODYSSEY_GALLERY_IMAGES)
    ? window.ODYSSEY_GALLERY_IMAGES
    : [];

  if (!items.length) {
    dynamicShowcaseMount.innerHTML = "";
    return;
  }

  dynamicShowcaseMount.innerHTML = items
    .map((item) => {
      const src = typeof item === "string" ? item : item.src;
      const title = typeof item === "string" ? prettifyFileName(item.split("/").pop()) : prettifyFileName(item.title || item.src.split("/").pop());
      const alt = typeof item === "string" ? title : item.alt || title;

      return `
        <article class="showcase-card reveal">
          <div class="showcase-copy">
            <span>Added from your gallery</span>
            <h3>${title}</h3>
          </div>
          <img src="${src}" alt="${alt}">
        </article>
      `;
    })
    .join("");

  dynamicShowcaseMount.querySelectorAll(".reveal").forEach((element) => observer.observe(element));
}

function t(key) {
  const language = body.dataset.language === "ar" ? "ar" : "en";
  return translations[language][key] || translations.en[key] || "";
}

function normalizeText(value) {
  return String(value || "")
    .trim()
    .replace(/\s+/g, " ");
}

function sanitizeValue(value) {
  return normalizeText(value).replace(/[<>]/g, "");
}

function isValidEmail(value) {
  return /^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/i.test(value);
}

function submissionFingerprint() {
  const formData = new FormData(form);
  return ["name", "company", "email", "phone"]
    .map((key) => sanitizeValue(formData.get(key)).toLowerCase())
    .join("|");
}

function hasRecentDuplicate() {
  const lastSubmission = sessionStorage.getItem("odyssey-demo-last-submission");
  if (!lastSubmission) return false;

  try {
    const parsed = JSON.parse(lastSubmission);
    if (!parsed.key || !parsed.timestamp) return false;
    return parsed.key === submissionFingerprint() && Date.now() - parsed.timestamp < DUPLICATE_WINDOW_MS;
  } catch {
    return false;
  }
}

function rememberSubmission() {
  sessionStorage.setItem(
    "odyssey-demo-last-submission",
    JSON.stringify({
      key: pendingSubmissionKey,
      timestamp: Date.now()
    })
  );
}

function setMessage(state, text) {
  formMessage.dataset.state = state;
  formMessage.textContent = text;
}

function setLoadingState(loading) {
  isSubmitting = loading;
  submitButton.disabled = loading;
  submitButton.classList.toggle("is-loading", loading);
  fields.forEach((field) => {
    field.readOnly = loading;
  });
}

function clearValidationErrors() {
  fields.forEach((field) => field.setAttribute("aria-invalid", "false"));
}

function markInvalid(field) {
  field.setAttribute("aria-invalid", "true");
}

function validateForm() {
  clearValidationErrors();

  const requiredFields = ["name", "company", "email", "phone"];
  let firstInvalidField = null;

  requiredFields.forEach((name) => {
    const field = form.elements.namedItem(name);
    if (!field || sanitizeValue(field.value)) return;
    markInvalid(field);
    firstInvalidField = firstInvalidField || field;
  });

  const emailField = form.elements.namedItem("email");
  if (emailField && sanitizeValue(emailField.value) && !isValidEmail(sanitizeValue(emailField.value))) {
    markInvalid(emailField);
    firstInvalidField = firstInvalidField || emailField;
    setMessage("error", t("formInvalidEmail"));
    emailField.focus();
    return false;
  }

  if (firstInvalidField) {
    setMessage("error", t("formInvalidRequired"));
    firstInvalidField.focus();
    return false;
  }

  return true;
}

function injectHiddenField(name, value) {
  let field = form.querySelector(`input[type="hidden"][name="${name}"]`);
  if (!field) {
    field = document.createElement("input");
    field.type = "hidden";
    field.name = name;
    form.appendChild(field);
  }
  field.value = value;
}

function prepareHiddenFields() {
  const payload = new FormData(form);
  const name = sanitizeValue(payload.get("name"));
  const company = sanitizeValue(payload.get("company"));
  const email = sanitizeValue(payload.get("email"));
  const phone = sanitizeValue(payload.get("phone"));

  injectHiddenField("jobTitle", "");
  injectHiddenField("country", "");
  injectHiddenField("companySize", "");
  injectHiddenField("industry", "");
  injectHiddenField("message", "");
  injectHiddenField("source", "Landing Page");
  injectHiddenField("status", "New");
  injectHiddenField("assignedTo", "");
  injectHiddenField("pageUrl", window.location.href);
  injectHiddenField("pageTitle", document.title);
  injectHiddenField("language", body.dataset.language === "ar" ? "ar" : "en");
  injectHiddenField("submittedAtClient", new Date().toISOString());
  injectHiddenField("dedupeKey", [name, company, email, phone].join("|").toLowerCase());
}

function resetFormState() {
  clearTimeout(submitTimeoutId);
  submitTimeoutId = null;
  pendingSubmissionKey = "";
  setLoadingState(false);
}

function handleSubmissionResponse(payload) {
  if (!payload || payload.type !== "odyssey-demo-response") return;

  resetFormState();

  if (payload.status === "success") {
    rememberSubmission();
    form.reset();
    clearValidationErrors();
    setMessage("success", t("formSuccess"));
    return;
  }

  if (payload.status === "duplicate") {
    setMessage("error", t("formDuplicate"));
    return;
  }

  setMessage("error", payload.message || t("formError"));
}

function isExpectedMessageOrigin(origin) {
  return /google(?:usercontent)?\.com$/i.test(new URL(origin).hostname);
}

window.addEventListener("message", (event) => {
  if (!event.origin) return;

  let payload = event.data;
  if (typeof payload === "string") {
    try {
      payload = JSON.parse(payload);
    } catch {
      return;
    }
  }

  if (!payload || payload.type !== "odyssey-demo-response") return;
  if (!isExpectedMessageOrigin(event.origin)) return;

  handleSubmissionResponse(payload);
});

form.addEventListener("submit", (event) => {
  event.preventDefault();

  if (isSubmitting) return;

  if (!APP_SCRIPT_URL || APP_SCRIPT_URL.includes("PASTE_YOUR_GOOGLE_APPS_SCRIPT_WEB_APP_URL_HERE")) {
    setMessage("error", t("formConfigError"));
    return;
  }

  if (!validateForm()) {
    return;
  }

  if (hasRecentDuplicate()) {
    setMessage("error", t("formDuplicate"));
    return;
  }

  pendingSubmissionKey = submissionFingerprint();
  prepareHiddenFields();
  setLoadingState(true);
  setMessage("loading", t("formLoading"));

  form.action = APP_SCRIPT_URL;
  form.method = "POST";
  form.target = hiddenFrame.name;

  submitTimeoutId = window.setTimeout(() => {
    resetFormState();
    setMessage("error", t("formError"));
  }, RESPONSE_TIMEOUT_MS);

  form.submit();
});

function applyLanguage(language) {
  const content = translations[language];
  if (!content) return;

  root.lang = language;
  body.dir = language === "ar" ? "rtl" : "ltr";
  body.dataset.language = language;
  langToggle.textContent = language === "ar" ? "English" : "العربية";

  document.querySelectorAll("[data-i18n]").forEach((element) => {
    const key = element.dataset.i18n;
    if (content[key]) {
      element.textContent = content[key];
    }
  });

  document.querySelectorAll("[data-i18n-placeholder]").forEach((element) => {
    const key = element.dataset.i18nPlaceholder;
    if (content[key]) {
      element.setAttribute("placeholder", content[key]);
    }
  });

  if (formMessage.dataset.state === "idle") {
    formMessage.textContent = content.formNote;
  }

  localStorage.setItem("odyssey-language", language);
}

function applyTheme(theme) {
  body.dataset.theme = theme;
  localStorage.setItem("odyssey-theme", theme);
}

langToggle.addEventListener("click", () => {
  applyLanguage(body.dataset.language === "ar" ? "en" : "ar");
});

themeToggle.addEventListener("click", () => {
  applyTheme(body.dataset.theme === "dark" ? "light" : "dark");
});

menuToggle.addEventListener("click", () => {
  nav.classList.toggle("is-open");
});

nav.querySelectorAll("a").forEach((link) => {
  link.addEventListener("click", () => nav.classList.remove("is-open"));
});

fields.forEach((field) => {
  field.addEventListener("input", () => {
    field.setAttribute("aria-invalid", "false");
    if (formMessage.dataset.state === "error") {
      setMessage("idle", t("formNote"));
    }
  });
});

const observer = new IntersectionObserver(
  (entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add("is-visible");
        observer.unobserve(entry.target);
      }
    });
  },
  { threshold: 0.16 }
);

document.querySelectorAll(".reveal").forEach((element) => observer.observe(element));

renderAutoGallery();
applyLanguage(savedLanguage);
applyTheme(savedTheme || (systemPrefersDark ? "dark" : "light"));
